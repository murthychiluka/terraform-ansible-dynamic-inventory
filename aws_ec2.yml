plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  tag:Role: webserver
  instance-state-name: running

keyed_groups:
  - key: ec2_tags.Role
    prefix: tag

compose:
  ansible_host: private_ip_address
