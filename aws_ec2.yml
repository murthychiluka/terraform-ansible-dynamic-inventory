# aws_ec2.yml
plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

filters:
  tag:Role: webserver
  instance-state-name: running

keyed_groups:
  - key: tags.Role
    prefix: tag

hostnames:
  - ip-address

compose:
  ansible_host: public_ip_addressgit